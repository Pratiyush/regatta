{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capabilities for the main window. Permissions are added per-feature as M1+ slices need them — no blanket scopes.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
