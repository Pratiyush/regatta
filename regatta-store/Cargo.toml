[package]
name = "regatta_store"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "The flow store: durable session metadata in SQLite (rusqlite, bundled)."
publish = false

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
