# Rust
/target
**/target
*.profraw
lcov.info
coverage/

# Node / frontend
node_modules/
dist/
.vite/

# OS
.DS_Store

# Regatta runtime data + reference clones (per-user; never committed)
# .ref is dot-prefixed so the revitify code-graph auto-skips it (dot-dirs are excluded).
.worktrees/
.ref/

# dev-spec-kit / graphify / revitify (derived, regenerated)
graphify-out/
.graphify/
.revitify/
.dev-spec-kit/cache/
.dev-spec-kit/tmp/
.dev-spec-kit/cockpit/
