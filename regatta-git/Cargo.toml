[package]
name = "regatta_git"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Run the git CLI in a working directory and parse its output (status, diff) via regatta_core."
publish = false

[dependencies]
regatta_core = { path = "../regatta-core" }
